import tokens from './tokens'

const DarkTheme = {
  '--Default-color-dark': tokens.light,
  '--Default-color-light': tokens.dark,
  '--Default-color-grey-light': tokens.dark,
  '--Default-color-grey-dark': tokens.greyLight,
  '--Default-color-blue': tokens.blue,
  '--Default-color-blue-light': tokens.blueLight,

  '--WeatherReport-Card-backgroundColor': tokens.greyDark
}

export default DarkTheme